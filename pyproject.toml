[project]
name = "ernie-image-skill"
version = "1.0.0"
description = "ERNIE-Image skill for Claude Code - generate images via Baidu AI Studio"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.33.0",
]
