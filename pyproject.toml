[project]
name = "ernie-image-skill"
version = "1.1.0"
description = "Claude Code skill for generating images with Baidu AI Studio ERNIE-Image models"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.33.0",
]
